GEMINI_API_KEY=
NOTIFICATION_EMAIL=
SMTP_HOST=
SMTP_PASS=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=

# --- AWS Database Configuration (For Vercel + AWS) ---
# Option 1: AWS DynamoDB (Recommended for Vercel serverless - fast, free tier, no connection pools)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DYNAMODB_TABLE=coreenact_enquiries

# Option 2: AWS RDS / Aurora PostgreSQL (Alternative if you prefer a relational database)
# DATABASE_URL=postgres://user:password@your-rds-instance.xxxx.us-east-1.rds.amazonaws.com:5432/coreenact
# DB_SSL=true
