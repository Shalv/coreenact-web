# Gemini API key for server-side AI chat, customer queries, maps grounding, and multimodal tools
GEMINI_API_KEY=

# Inquiries notification recipient (defaults to info@coreenact.com if empty)
NOTIFICATION_EMAIL=

# Optional SMTP configuration for live email dispatch
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=
